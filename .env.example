# Copy to .env and fill in before running `docker compose up --build`.
POSTGRES_PASSWORD=change-me
# Leave empty for a phone-only setup (the Android app sends no browser Origin header).
ALLOWED_ORIGINS=
